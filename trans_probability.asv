function [trans_probs, transition_tuple] = trans_probability(data, N, M)
    discrete = zeros(N, M);
    tups = zeros((M-1)*(N-1), 3);
    discrete(data >=0.5) = 1;
    tups_idx = 0;
    for n = 1:(N-1)
        for m = 1:(M - 1)
            tups()
        end

    end
end
